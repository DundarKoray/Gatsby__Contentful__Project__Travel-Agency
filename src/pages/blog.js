import React from 'react';
import Layout from '../components/Layout'

const blog = () => {
    return (
        <Layout>
            hello from blog page
        </Layout>
    );
};

export default blog;