import React from 'react';
import Layout from './components/Layout'

const error = () => {
    return (
        <Layout>
            hello from error
        </Layout>
    );
};

export default error;